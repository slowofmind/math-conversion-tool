\documentclass{standalone}
\usepackage{plotstyle}

\begin{document}

\begin{tikzpicture}
    \begin{groupplot}[
        group style = {
            group size = 4 by 1,
            % horizontal sep = 1.5cm,
            vertical sep = 1.5cm,
        },
        width = 5cm,
        height = 5cm,
        xlabel = {$x$},
        ylabel = {$y$},
        enlargelimits = true,
        xtick = {-3, -2, -1, 1, 2, 3},
        ytick = {-3, -2, -1, 1, 2, 3},
        xticklabels = \empty,
        yticklabels=\empty,
        grid=major,
        xmin=-3.5, xmax=3.5, ymin=-3.5, ymax=3.5,
        clip=false
    ]

    \nextgroupplot
    \node at (rel axis cs:0.5,1.2) {(a)};
    \addplot[draw=none] {x};
    \def\radius{2} \def\startAngle{180} \def\endAngle{270}
    \draw[thick, ->] (\startAngle:\radius) arc[start angle=\startAngle, end angle=\endAngle, radius=\radius];
    \fill (\startAngle:\radius) circle (2pt); % Start point
    \fill (\endAngle:\radius) circle (2pt); % End point
    \draw[dotted] (0,0) circle (\radius);
    \draw[->, very thick] (0,0) -- (\startAngle:3);
    \draw[->, very thick] (0,0) -- (\endAngle:3);

    \nextgroupplot
    \node at (rel axis cs:0.5,1.2) {(b)};
    \addplot[draw=none] {x};
    \def\radius{2.83} \def\startAngle{45} \def\endAngle{135}
    \draw[thick, ->] (\startAngle:\radius) arc[start angle=\startAngle, end angle=\endAngle, radius=\radius];
    \fill (\startAngle:\radius) circle (2pt); % Start point
    \fill (\endAngle:\radius) circle (2pt); % End point
    \draw[dotted] (0,0) circle (\radius);
    \draw[->, very thick] (0,0) -- (\startAngle:4);
    \draw[->, very thick] (0,0) -- (\endAngle:4);

    \nextgroupplot
    \node at (rel axis cs:0.5,1.2) {(c)};
    \addplot[draw=none] {x};
    \def\radius{2} \def\startAngle{0} \def\endAngle{135}
    \draw[thick, ->] (\startAngle:\radius) arc[start angle=\startAngle, end angle=\endAngle, radius=\radius];
    \fill (\startAngle:\radius) circle (2pt); % Start point
    \fill (\endAngle:\radius) circle (2pt); % End point
    \draw[dotted] (0,0) circle (\radius);
    \draw[->, very thick] (0,0) -- (\startAngle:3);
    \draw[->, very thick] (0,0) -- (\endAngle:3);

    \nextgroupplot
    \node at (rel axis cs:0.5,1.2) {(d)};
    \addplot[draw=none] {x};
    \def\radius{2} \def\startAngle{0} \def\endAngle{-90}
    \draw[thick, ->] (\startAngle:\radius) arc[start angle=\startAngle, end angle=\endAngle, radius=\radius];
    \fill (\startAngle:\radius) circle (2pt); % Start point
    \fill (\endAngle:\radius) circle (2pt); % End point
    \draw[dotted] (0,0) circle (\radius);
    \draw[->, very thick] (0,0) -- (\startAngle:3);
    \draw[->, very thick] (0,0) -- (\endAngle:3);
    \end{groupplot}
\end{tikzpicture}

\end{document}


    % First plot with >360° rotation
    \nextgroupplot
    \def\radius{1} \def\startAngle{0} \def\endAngle{360} \def\extraAngle{90}
    \node at (rel axis cs:0.5,1.05) {(a)};
    \draw[thick, ->] (\startAngle:\radius) arc[start angle=\startAngle, end angle=\endAngle, radius=\radius];
    \draw[thick, ->] (\endAngle:\radius) arc[start angle=\endAngle, end angle=\endAngle+\extraAngle, radius=\radius];
    \fill[red] (\startAngle:\radius) circle (2pt); % Start point
    \fill[blue] (\endAngle+\extraAngle:\radius) circle (2pt); % End point
    \draw[dashed] (0,0) -- (\startAngle:\radius+0.5);
    \draw[dashed] (0,0) -- (\endAngle+\extraAngle:\radius+0.5);
    \draw[decorate,decoration={brace,amplitude=4pt,mirror,raise=3pt}] 
        (\startAngle:\radius) -- (0,0) node[midway,anchor=north east] {$r=\radius$};

    % Second plot
    \nextgroupplot
    \def\radius{2} \def\startAngle{30} \def\endAngle{150}
    \node at (rel axis cs:0.5,1.05) {(b)};
    \draw[thick, ->] (\startAngle:\radius) arc[start angle=\startAngle, end angle=\endAngle, radius=\radius];
    \fill[red] (\startAngle:\radius) circle (2pt); % Start point
    \fill[blue] (\endAngle:\radius) circle (2pt); % End point
    \draw[dashed] (0,0) -- (\startAngle:\radius+0.5);
    \draw[dashed] (0,0) -- (\endAngle:\radius+0.5);
    \draw[decorate,decoration={brace,amplitude=4pt,mirror,raise=3pt}] 
        (\startAngle:\radius) -- (0,0) node[midway,anchor=north east] {$r=\radius$};

    % Third plot
    \nextgroupplot
    \def\radius{3} \def\startAngle{60} \def\endAngle{180}
    \node at (rel axis cs:0.5,1.05) {(c)};
    \draw[thick, ->] (\startAngle:\radius) arc[start angle=\startAngle, end angle=\endAngle, radius=\radius];
    \fill[red] (\startAngle:\radius) circle (2pt); % Start point
    \fill[blue] (\endAngle:\radius) circle (2pt); % End point
    \draw[dashed] (0,0) -- (\startAngle:\radius+0.5);