\documentclass[12pt,letterpaper]{article}



\input{../worksheet-setup.tex}
\usetikzlibrary{arrows}
\usepackage{wrapfig}
\usepackage{amsfonts,amsmath}

\makeatletter
\renewcommand*\env@matrix[1][*\c@MaxMatrixCols c]{%
	\hskip -\arraycolsep
	\let\@ifnextchar\new@ifnextchar
	\array{#1}}
\makeatother

\def\env@matrix{\hskip -\arraycolsep
	\let\@ifnextchar\new@ifnextchar
	\array{*\c@MaxMatrixCols c}}

\newcommand{\norm}[1]{\left\lVert#1\right\rVert}

\chead{\Large\textbf{Inner Product Spaces, $\vec\S$6.7}}% 

\begin{document}
\thispagestyle{fancy}

Recall from previously:

\begin{itemize}
\item {\bf Gram-Schmidt Orthogonalization.} Given a basis $\{x_1, \dots, x_p\}$ for a non-zero subspace $W$ of $\mathbb{R}^n$, define 
\begin{align*}
v_1 & = x_1\\
v_2 & = x_2 - \left( \frac{x_2 \cdot v_1}{v_1 \cdot v_1} \right) v_1\\
v_3 & = x_3 -\left( \frac{x_3 \cdot v_1}{v_1 \cdot v_1} \right) v_1-\left( \frac{x_3 \cdot v_2}{v_2 \cdot v_2} \right) v_2\\
&\vdots\\
v_p & = x_p -\left( \frac{x_p \cdot v_1}{v_1 \cdot v_1} \right) v_1-\dots -\left( \frac{x_p \cdot v_{p-1}}{v_{p-1} \cdot v_{p-1}} \right) v_{p-1}.
\end{align*}
Then $\{v_1, \dots, v_p\}$ is an orthogonal basis for $W$. In addition $\text{Span}\{v_1,\dots, v_k\}=\text{Span}\{ x_1, \dots, x_k\}$ for all $1 \leq k \leq p$.
\item {\bf QR Factorization.} If $A$ is an $m\times n$ matrix with linearly independent columns, then $A=QR$ where $Q$ is an $m\times n$ matrix whose columns form an orthonormal basis for the column space of $A$ and $R$ is an upper triangular $n \times n$ matrix with positive entries along the diagonal. 
\item A {\bf least squares solution} to $Ax=b$ is a vector $\hat{x} \in \mathbb{R}^n$ such that $$\norm{b-A\hat{x}} \leq \norm{b-Ax}$$ for all $x \in \mathbb{R}^n$.
\item {\bf Theorem 6.13:} The set of least squares solutions to $Ax=b$ is the same as the set of solutions to the normal squares $A^{T}Ax=A^{T}b$.
\item The {\bf least squares error} is given by the distance from $b$ to $A\hat{x}$.
\end{itemize}

\newpage

\begin{defn}
	An \textbf{Inner Product Space} (over $\mathbb{R}$) is a vector space $V$ with a pairing (called \textbf{the inner product}) $\langle\cdot,\cdot\rangle:V^2 \rightarrow \mathbb{R},$ that satisfies the following: 
	
	\begin{itemize}
		\item \textbf{Linearity in the first factor:}\\ $\langle  c_1 f_1 + c_2 f_2, g\rangle = c_1 \langle f_1, g\rangle + c_2 \langle f_2, g\rangle$.		
		\item \textbf{Symmetry:} $\langle g, f\rangle = \langle f, g\rangle$.
		
		\item \textbf{Positive definiteness:} $\langle f, f\rangle \geq 0$, 
		with $\langle f, f\rangle = 0$ if and only if $f = 0.$
		
		%	\item (A theorem, not an independent axiom)\\
		%	Conjugate linearity in the second factor: $\langle f, cg\rangle = %\overline{c}\langle f,g\rangle$
		
	\end{itemize}
\end{defn}


\begin{enumerate}

 \Problem A few examples of inner product spaces.
\begin{enumerate}
	
	\item The set $\mathbb{R}^n$ with the inner product $\langle \vec{x}, \vec{y}\rangle =\vec{x} \cdot \vec{y}$.
	
	\item The set of polynomials of degree 4 or less, $\mathbb{P}_4$ with $$\langle p, q \rangle = p(-2)q(-2)+\cdots + p(2)q(2).$$
	
	\item The space $\ell^2$ of infinite sequences of real numbers $\vec{x}=(x_{1},x_{2},\ldots,x_{n},\ldots)$ for which $\sum x_j^2 $ converges, with inner product,
	$$\langle \vec{x},\vec{y}\rangle  = \sum_{j=1}^{\infty}x_j y_j.$$
	
	\item The space $\mathrm{C}[a,b]$ of real valued continuous functions with domain $[a,b]$.	The inner product is:	
	$$\langle f,g\rangle  = \int_{a}^b f(x)g(x) dx.$$
	
\end{enumerate}

\newpage

\Problem Let $V=C[-1,1]$ with inner product defined above. Let $W=\text{Span}\{1, t, t^2\}$.
\Part Find an orthogonal basis for $W$.
  
  \vfill
  
\Part Find the best approximation to $e^t$ in $W$.

\vfill

\newpage

Consider $f\in C[0, 2 \pi]$, then we want to approximate $f$ using sinusoidal functions. The set $$W_n=\{1, \cos{t}, \dots, \cos{nt}, \sin{t}, \dots, \sin{nt}\}$$ is orthogonal with respect to $$\langle f,g \rangle = \int_0^{2\pi} f(t) g(t) dt.$$

\begin{defn} The $n$-th order {\bf Fourier approximation} to $f\in C[0,2\pi]$ is given by the $\text{proj}_{W_n}{f}$.
\end{defn}

{\bf Note:}   For $k>0$, the {\bf Fourier coefficients} $a_k$ and $b_k$ are given by $$a_k=\frac{\langle f, \cos{kt} \rangle}{\langle \cos{kt}, \cos{kt}\rangle} \text{, and } b_k=\frac{\langle f, \sin{kt} \rangle}{\langle \sin{kt}, \sin{kt}\rangle},$$ and hence $$a_k=\frac{1}{\pi}\int_0^{2\pi}{f(t)\cos{kt}dt } \text{, and } b_k=\frac{1}{\pi}\int_{0}^{2\pi}{f(t)\sin{kt}dt}.$$



\Problem Find the $n$-th order Fourier approximation for $f(t)=t$ on $[0,2\pi]$.

\vfill

{\bf Remark:} More generally, the {\bf Fourier series} is $$f(t)=\frac{a_0}{2}+\sum_{m=1}^{\infty}{(a_m \cos{mt}+b_m \sin{mt})}.$$

\newpage

\end{enumerate}

\begin{comment}
\newpage
\chead{\Large\textbf{Inner Product Spaces -- Answers / Solutions}}%
\lhead{}
\rhead{}
\thispagestyle{fancy}

\begin{enumerate}
\Answer 
\Answer 
\begin{enumerate}
	\item We use Gram-Schmidt to find an orthogonal basis. Let $v_1=1$. Note $\langle 1, t \rangle=0$, and hence we take $v_2=t$. Let $W_2=\text{Span}\{1, t\}$.
	
	Note $\text{proj}_{W_2}=\frac{\langle t^2,1 \rangle }{\langle 1,1 \rangle} (1)+\frac{\langle t^2 ,t  \rangle}{\langle t, t\rangle} (t)=\frac{1}{3}.$ Thus $v_3=t^2-\frac{1}{3}$. We take the orthogonal basis $\{1, t, 3t^2-1\}$ for $W$.
	\item Here $\text{proj}_{W}{e^t}=\frac{\langle t^2,1 \rangle }{\langle 1,1 \rangle} (1)+\frac{\langle t^2 ,t  \rangle}{\langle t, t\rangle} (t)+\frac{\langle t^2,3t^2-1 \rangle }{\langle 3t^2-1,3t^2-1 \rangle} (3t^2-1)=\frac{33}{4e} -\frac{3e}{4} +\frac{3t}{e}-\frac{105t^2}{4e}+\frac{15et^2}{4}$.
\end{enumerate}
\Answer For $k>0$, we get
\begin{align*}
\frac{a_0}{2} & = \frac{1}{2\pi} \int_0^{2\pi} t dt= \pi\\
a_k & = \frac{1}{\pi}\int_0^{2\pi} t \cos{kt}dt=0\\
b_k & = \frac{1}{\pi}\int_0^{2\pi} t \sin{kt}dt=\frac{-2}{k}.\\
\end{align*}
Thus the $n$-th order Fourier series for $f(t)=t$ is given by $$\pi - 2 \sin{t}- \sin{2t} -\frac{2}{3} \sin{3t} - \dots - \frac{2}{n}\sin{nt}.$$
\end{enumerate}  

\end{comment}

\end{document}


%%% Local ables: 
%%% mode: latex
%%% TeX-master: t
%%% End: 
