# LuaLaTeX Compilation Pipeline
# Triggered when .tex files are committed to pipeline-lualatex-input/
# Runs LuaLaTeX in Docker, commits PDF to pipeline-output/ and creates a Release.
#
# The browser tool fetches the PDF from pipeline-output/ via the Contents API
# using Accept: application/vnd.github.raw (raw binary, no base64).
# The Release serves as a backup and direct-access point.

name: LuaLaTeX Compile

on:
  push:
    paths:
      - 'pipeline-lualatex-input/**'

permissions:
  contents: write
  packages: read

jobs:
  compile:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Verify input files
        run: |
          echo "Files to compile:"
          ls -lh pipeline-lualatex-input/*.tex 2>/dev/null || echo "No .tex files found"

      - name: Create output directory
        run: mkdir -p pipeline-output

      - name: Run LuaLaTeX pipeline
        run: |
          docker run --rm \
            -e MODE=lualatex \
            -v "${{ github.workspace }}/pipeline-lualatex-input:/source" \
            -v "${{ github.workspace }}/pipeline-output:/output" \
            ghcr.io/slowofmind/latexml-pipeline:latest

      - name: Show output files
        run: ls -lh pipeline-output/

      - name: Commit results and clean up input
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # Stage output files (PDF + logs)
          git add pipeline-output/
          
          # Remove processed input files to keep repo clean
          git rm pipeline-lualatex-input/*.tex 2>/dev/null || true
          
          # Commit with [skip ci] to prevent re-triggering
          git commit -m "LuaLaTeX compilation output [skip ci]" || echo "No changes to commit"
          git push

      - name: Create release with PDF (backup)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Build a unique tag from the commit timestamp
          TAG="lualatex-$(date +%s)"
          
          # Find the PDF file(s)
          PDF_FILES=$(ls pipeline-output/*.pdf 2>/dev/null)
          if [ -z "$PDF_FILES" ]; then
            echo "::warning::No PDF files produced"
            exit 0
          fi
          
          # Extract base name for the release title
          BASENAME=$(basename pipeline-output/*.pdf .pdf 2>/dev/null | head -1)
          
          # Create the release with all PDF and log files attached
          gh release create "$TAG" \
            pipeline-output/*.pdf \
            pipeline-output/*-lualatex.log \
            pipeline-output/*-engine.log \
            --title "LuaLaTeX: ${BASENAME}" \
            --notes "Compiled from ${BASENAME}.tex on $(date -u '+%Y-%m-%d %H:%M UTC')" \
            --latest=false \
            2>/dev/null || \
          gh release create "$TAG" \
            pipeline-output/*.pdf \
            --title "LuaLaTeX: ${BASENAME}" \
            --notes "Compiled from ${BASENAME}.tex on $(date -u '+%Y-%m-%d %H:%M UTC')" \
            --latest=false \
            2>/dev/null || echo "::warning::Release creation failed (non-fatal)"
